export const state = {
  isLoading: false
};
const getters = {
  appIsLoading: state => state.isLoading
};
const actions = {};
export const mutations = {};
export default {
  state,
  getters,
  actions,
  mutations
};
